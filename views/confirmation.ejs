<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Confirmation</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        /* Full-page centering */
        body, html {
            height: 100%;
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f8f9fa;
        }
        /* Bigger Confirmation Box */
        .confirmation-container {
            width: 60%; /* Increase size */
            max-width: 700px; /* Prevent it from being too large */
            padding: 40px;
            background: white;
            border-radius: 20px;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.15);
            text-align: center;
            animation: fadeIn 0.8s ease-in-out;
        }
        /* Bigger Emoji */
        .emoji {
            font-size: 90px;
            display: block;
            margin-bottom: 20px;
            animation: bounce 1s infinite alternate;
        }
        /* Stylish Button */
        .btn-primary {
            padding: 15px 30px;
            border-radius: 50px;
            font-size: 18px;
            font-weight: bold;
        }
        /* Heading Size */
        .fw-bold {
            font-size: 30px;
        }
        /* Text Size */
        .text-muted {
            font-size: 18px;
        }
        /* Fade-in Animation */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        /* Bouncing Effect */
        @keyframes bounce {
            from { transform: translateY(0); }
            to { transform: translateY(-10px); }
        }
    </style>
</head>
<body>

    <%- include('partials/header') %>

    <div class="confirmation-container">
        <span class="emoji">🎉</span>
        <h2 class="fw-bold text-success">Payment Successful!</h2>
        <p class="text-muted">Thank you for shopping with us. Your order is being processed.</p>
        <a href="/clothingproducts" class="btn btn-primary mt-4">Continue Shopping</a>
    </div>

    <%- include('partials/footer') %>

</body>
</html>
